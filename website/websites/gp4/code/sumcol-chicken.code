<span class="hl slc">;;; The Computer Language Benchmarks Game</span>
<span class="hl slc">;;; http://shootout.alioth.debian.org/</span>
<span class="hl slc">;;;</span>

<span class="hl sym">(</span>define <span class="hl sym">(</span>main<span class="hl sym">)</span>
  <span class="hl sym">(</span>let <span class="hl sym">((</span>sum <span class="hl num">0</span><span class="hl sym">))</span>
    <span class="hl sym">(</span>do <span class="hl sym">((</span>myInt <span class="hl sym">(</span><span class="hl kwa">read</span>-<span class="hl kwa">line</span><span class="hl sym">) (</span><span class="hl kwa">read</span>-<span class="hl kwa">line</span><span class="hl sym">)))</span>
	<span class="hl sym">((</span>eof-object? myInt<span class="hl sym">) (</span><span class="hl kwa">print</span> sum<span class="hl sym">))</span>
      <span class="hl sym">(</span><span class="hl kwa">set</span><span class="hl sym">!</span> sum <span class="hl sym">(+</span> sum <span class="hl sym">(</span>string-<span class="hl sym">&gt;</span>number myInt<span class="hl sym">))))))</span>

<span class="hl sym">(</span>main<span class="hl sym">)</span>

