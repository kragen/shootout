<span class="hl com">/*</span>
<span class="hl com"> * The Computer Language Shootout</span>
<span class="hl com"> * http://shootout.alioth.debian.org/</span>
<span class="hl com"> *</span>
<span class="hl com"> * contributed by David Pollak</span>
<span class="hl com"> */</span>

<span class="hl kwa">import</span> java<span class="hl sym">.</span>io<span class="hl sym">.</span><span class="hl kwc">InputStream</span>

<span class="hl kwa">object</span> sumcol
<span class="hl sym">{</span>
  <span class="hl kwa">def</span> main<span class="hl sym">(</span>args<span class="hl sym">:</span> <span class="hl kwc">Array</span><span class="hl sym">[</span><span class="hl kwc">String</span><span class="hl sym">]) =</span>
  <span class="hl sym">{</span>
    <span class="hl kwa">def</span> sumIn<span class="hl sym">(</span>cur<span class="hl sym">:</span> <span class="hl kwb">int</span><span class="hl sym">,</span> sum<span class="hl sym">:</span> <span class="hl kwb">int</span><span class="hl sym">,</span> in<span class="hl sym">:</span> <span class="hl kwc">InputStream</span><span class="hl sym">):</span> <span class="hl kwb">int</span> <span class="hl sym">= {</span>
      <span class="hl kwa">val</span> i <span class="hl sym">=</span> in<span class="hl sym">.</span>read
      <span class="hl kwa">if</span> <span class="hl sym">(</span>i <span class="hl sym">== -</span><span class="hl num">1</span><span class="hl sym">)</span> sum
      <span class="hl kwa">else</span> <span class="hl sym">{</span>
        <span class="hl kwa">val</span> c <span class="hl sym">=</span> i<span class="hl sym">.</span>toChar
        <span class="hl kwa">if</span> <span class="hl sym">(</span>c <span class="hl sym">&gt;=</span> <span class="hl str">'0'</span> <span class="hl sym">&amp;&amp;</span> c <span class="hl sym">&lt;=</span><span class="hl str">'9'</span><span class="hl sym">)</span> sumIn<span class="hl sym">(</span>cur <span class="hl sym">*</span> <span class="hl num">10</span> <span class="hl sym">+ (</span>c <span class="hl sym">-</span> <span class="hl str">'0'</span><span class="hl sym">),</span> sum<span class="hl sym">,</span> in<span class="hl sym">)</span>
        <span class="hl kwa">else</span> sumIn<span class="hl sym">(</span><span class="hl num">0</span><span class="hl sym">,</span> sum <span class="hl sym">+</span> cur<span class="hl sym">,</span> in<span class="hl sym">)</span>
      <span class="hl sym">}</span>
    <span class="hl sym">}</span>

    Console<span class="hl sym">.</span>println<span class="hl sym">(</span>sumIn<span class="hl sym">(</span><span class="hl num">0</span><span class="hl sym">,</span><span class="hl num">0</span><span class="hl sym">,</span> <span class="hl kwc">System</span><span class="hl sym">.</span>in<span class="hl sym">))</span>
  <span class="hl sym">}</span>
<span class="hl sym">}</span>
