<span class="hl slc">#!/usr/bin/tclsh</span>
<span class="hl slc"># The Computer Language Shootout</span>
<span class="hl slc"># http://shootout.alioth.debian.org/</span>
<span class="hl slc"># contributed by Donald Arseneau</span>

<span class="hl kwa">proc</span> main <span class="hl sym">{} {</span>
    <span class="hl kwa">set</span> sum <span class="hl num">0</span>
    <span class="hl kwa">catch</span> <span class="hl sym">{</span>
        <span class="hl kwa">while</span> <span class="hl sym">{</span><span class="hl num">1</span><span class="hl sym">} {</span> <span class="hl kwa">incr</span> sum <span class="hl sym">[</span><span class="hl kwa">gets</span> stdin<span class="hl sym">] }</span>
    <span class="hl sym">}</span>
    <span class="hl kwa">puts</span> <span class="hl kwb">$sum</span>
<span class="hl sym">}</span>

main
