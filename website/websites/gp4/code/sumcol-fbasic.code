rem The Computer Language Shootout
rem http<span class="sym">://</span>shootout.alioth.debian.org<span class="sym">/</span>
rem contributed by Josh Goldfoot

file_handle <span class="sym">=</span> freefile<span class="sym">()</span>
open cons <span class="kwa">as</span> file_handle

total <span class="sym">=</span> <span class="num">0</span>
loop1<span class="sym">:</span>
   input #file_handle<span class="sym">,</span> a$
   <span class="kwa">if</span> a$ <span class="sym">&lt;&gt;</span> <span class="str">&quot;&quot;</span> <span class="kwa">then</span>
      total <span class="sym">=</span> total <span class="sym">+</span> valint<span class="sym">(</span>a$<span class="sym">)</span>
      goto loop1
   <span class="kwa">end if</span>

print str<span class="sym">(</span>total<span class="sym">)</span>

