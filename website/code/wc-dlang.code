<span class="com">/* The Great Computer Language Shootout
   http://shootout.alioth.debian.org/

   contributed by Dave Fladebo
   compile: dmd -O -inline -release wc.d
*/</span>

import std<span class="sym">.</span>stream<span class="sym">;

</span><span class="kwb">int</span> main<span class="sym">()
{
</span>    <span class="kwb">int</span>  nl<span class="sym">,</span> nw<span class="sym">,</span> nc<span class="sym">,</span> nread<span class="sym">,</span> inword<span class="sym">;
</span>    <span class="kwb">char</span><span class="sym">[</span><span class="num">4096</span><span class="sym">]</span> buf<span class="sym">;

</span>    <span class="kwa">while</span><span class="sym">((</span>nread <span class="sym">=</span> stdin<span class="sym">.</span>readBlock<span class="sym">(</span>buf<span class="sym">,</span> buf<span class="sym">.</span>length<span class="sym">)) &gt;</span> <span class="num">0</span><span class="sym">)
    {
</span>        nc <span class="sym">+=</span> nread<span class="sym">;
</span>        <span class="kwa">for</span><span class="sym">(</span><span class="kwb">int</span> idx <span class="sym">=</span> <span class="num">0</span><span class="sym">;</span> idx <span class="sym">&lt;</span> nread<span class="sym">;</span> idx<span class="sym">++)
        {
</span>            <span class="kwa">switch</span><span class="sym">(</span>buf<span class="sym">[</span>idx<span class="sym">])
            {
</span>                <span class="kwa">case</span> <span class="str">'</span><span class="esc">\n</span><span class="str">'</span><span class="sym">:
</span>                    nl<span class="sym">++;
</span>                <span class="kwa">case</span> <span class="str">' '</span><span class="sym">:
</span>                <span class="kwa">case</span> <span class="str">'</span><span class="esc">\t</span><span class="str">'</span><span class="sym">:
</span>                    nw <span class="sym">+=</span> inword<span class="sym">;
</span>                    inword <span class="sym">=</span> <span class="num">0</span><span class="sym">;
</span>                    <span class="kwa">break</span><span class="sym">;
</span>                <span class="kwa">default</span><span class="sym">:
</span>                    inword <span class="sym">=</span> <span class="num">1</span><span class="sym">;
</span>                    <span class="kwa">break</span><span class="sym">;
            }
        }
    }

</span>    stdout<span class="sym">.</span>writefln<span class="sym">(</span><span class="str">&quot;%d %d %d&quot;</span><span class="sym">,</span> nl<span class="sym">,</span> nw<span class="sym">,</span> nc<span class="sym">);

</span>    <span class="kwa">return</span> <span class="num">0</span><span class="sym">;
}</span>
