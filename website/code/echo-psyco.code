<span class="line">    1 </span><span class="slc">#!/usr/bin/python
</span><span class="line">    2 </span><span class="slc"></span><span class="slc"># $Id: echo-psyco.code,v 1.3 2004-11-11 07:13:56 bfulgham Exp $
</span><span class="line">    3 </span><span class="slc"></span><span class="slc"># http://www.bagley.org/~doug/shootout/
</span><span class="line">    4 </span><span class="slc"></span><span class="slc"># with help from Brad Knotwell
</span><span class="line">    5 </span><span class="slc"></span>
<span class="line">    6 </span><span class="kwc">import</span> sys<span class="sym">,</span> os<span class="sym">,</span> psyco
<span class="line">    7 </span><span class="kwc">from</span> socket <span class="kwc">import</span> <span class="sym">*
</span><span class="line">    8 </span><span class="sym"></span>psyco<span class="sym">.</span>full<span class="sym">()
</span><span class="line">    9 </span><span class="sym">
</span><span class="line">   10 </span><span class="sym"></span>DATA <span class="sym">=</span> <span class="str">&quot;Hello there sailor</span><span class="esc">\n</span><span class="str">&quot;</span>
<span class="line">   11 </span>bufferSize <span class="sym">=</span> <span class="kwa">len</span><span class="sym">(</span>DATA<span class="sym">)
</span><span class="line">   12 </span><span class="sym">
</span><span class="line">   13 </span><span class="sym"></span><span class="kwb">def</span> server_sock<span class="sym">():
</span><span class="line">   14 </span><span class="sym"></span>    sock <span class="sym">=</span> socket<span class="sym">(</span>AF_INET<span class="sym">,</span> SOCK_STREAM<span class="sym">)
</span><span class="line">   15 </span><span class="sym"></span>    sock<span class="sym">.</span>setsockopt<span class="sym">(</span>SOL_SOCKET<span class="sym">,</span> SO_REUSEADDR<span class="sym">,</span> <span class="num">1</span><span class="sym">)
</span><span class="line">   16 </span><span class="sym"></span>    sock<span class="sym">.</span>bind<span class="sym">((</span><span class="str">'127.0.0.1'</span><span class="sym">,</span> <span class="num">0</span><span class="sym">));
</span><span class="line">   17 </span><span class="sym"></span>    sock<span class="sym">.</span>listen<span class="sym">(</span><span class="num">2</span><span class="sym">)
</span><span class="line">   18 </span><span class="sym"></span>    <span class="kwb">return</span><span class="sym">(</span>sock<span class="sym">)
</span><span class="line">   19 </span><span class="sym">
</span><span class="line">   20 </span><span class="sym"></span><span class="kwb">def</span> get_port<span class="sym">(</span>sock<span class="sym">):
</span><span class="line">   21 </span><span class="sym"></span>    host<span class="sym">,</span> port <span class="sym">=</span> sock<span class="sym">.</span>getsockname<span class="sym">()
</span><span class="line">   22 </span><span class="sym"></span>    <span class="kwb">return</span><span class="sym">(</span>port<span class="sym">)
</span><span class="line">   23 </span><span class="sym">
</span><span class="line">   24 </span><span class="sym"></span><span class="kwb">def</span> client_sock<span class="sym">(</span>port<span class="sym">):
</span><span class="line">   25 </span><span class="sym"></span>    sock <span class="sym">=</span> socket<span class="sym">(</span>AF_INET<span class="sym">,</span> SOCK_STREAM<span class="sym">)
</span><span class="line">   26 </span><span class="sym"></span>    sock<span class="sym">.</span>connect<span class="sym">((</span><span class="str">'127.0.0.1'</span><span class="sym">,</span> port<span class="sym">))
</span><span class="line">   27 </span><span class="sym"></span>    <span class="kwb">return</span><span class="sym">(</span>sock<span class="sym">)
</span><span class="line">   28 </span><span class="sym">
</span><span class="line">   29 </span><span class="sym"></span><span class="kwb">def</span> echo_client<span class="sym">(</span>n<span class="sym">,</span> port<span class="sym">):
</span><span class="line">   30 </span><span class="sym"></span>    sock <span class="sym">=</span> client_sock<span class="sym">(</span>port<span class="sym">)
</span><span class="line">   31 </span><span class="sym"></span>    sender<span class="sym">,</span>receiver <span class="sym">=</span> sock<span class="sym">.</span>send<span class="sym">,</span>sock<span class="sym">.</span>recv
<span class="line">   32 </span>    <span class="kwb">for</span> i <span class="kwb">in</span> <span class="kwa">range</span><span class="sym">(</span><span class="num">0</span><span class="sym">,</span>n<span class="sym">):
</span><span class="line">   33 </span><span class="sym"></span>        sender<span class="sym">(</span>DATA<span class="sym">)
</span><span class="line">   34 </span><span class="sym"></span>        ans <span class="sym">=</span> receiver<span class="sym">(</span>bufferSize<span class="sym">)
</span><span class="line">   35 </span><span class="sym"></span>        <span class="kwb">while</span> ans<span class="sym">[-</span><span class="num">1</span><span class="sym">] !=</span> <span class="str">&quot;</span><span class="esc">\n</span><span class="str">&quot;</span><span class="sym">:
</span><span class="line">   36 </span><span class="sym"></span>            ans <span class="sym">+=</span> receiver<span class="sym">(</span>bufferSize <span class="sym">-</span> <span class="kwa">len</span><span class="sym">(</span>ans<span class="sym">))
</span><span class="line">   37 </span><span class="sym"></span>        <span class="kwb">if</span> ans <span class="sym">&lt;&gt;</span> DATA<span class="sym">:
</span><span class="line">   38 </span><span class="sym"></span>            <span class="kwb">raise</span><span class="sym">(</span><span class="str">&quot;client:</span> <span class="esc">\&quot;</span><span class="str">%s</span><span class="esc">\&quot; </span> <span class="str">ne</span> <span class="esc">\&quot;</span><span class="str">%s</span><span class="esc">\&quot;</span><span class="str">&quot;</span> <span class="sym">% (</span>DATA<span class="sym">,</span> ans<span class="sym">))
</span><span class="line">   39 </span><span class="sym"></span>    sock<span class="sym">.</span>close<span class="sym">()
</span><span class="line">   40 </span><span class="sym">
</span><span class="line">   41 </span><span class="sym"></span><span class="kwb">def</span> echo_server<span class="sym">(</span>n<span class="sym">):
</span><span class="line">   42 </span><span class="sym"></span>    ssock <span class="sym">=</span> server_sock<span class="sym">()
</span><span class="line">   43 </span><span class="sym"></span>    <span class="kwb">if</span> os<span class="sym">.</span>fork<span class="sym">() &gt;</span> <span class="num">0</span><span class="sym">:
</span><span class="line">   44 </span><span class="sym"></span>        <span class="slc"># parent is server
</span><span class="line">   45 </span><span class="slc"></span>        csock<span class="sym">,</span> addr <span class="sym">=</span> ssock<span class="sym">.</span>accept<span class="sym">()
</span><span class="line">   46 </span><span class="sym"></span>        n <span class="sym">=</span> <span class="num">0
</span><span class="line">   47 </span><span class="num"></span>        sender<span class="sym">,</span>receiver <span class="sym">=</span> csock<span class="sym">.</span>send<span class="sym">,</span>csock<span class="sym">.</span>recv
<span class="line">   48 </span>        <span class="kwb">while</span> <span class="num">1</span><span class="sym">:
</span><span class="line">   49 </span><span class="sym"></span>            dat <span class="sym">=</span> receiver<span class="sym">(</span>bufferSize<span class="sym">)
</span><span class="line">   50 </span><span class="sym"></span>            <span class="kwb">if not</span> dat<span class="sym">:</span> <span class="kwb">break
</span><span class="line">   51 </span><span class="kwb"></span>            sender<span class="sym">(</span>dat<span class="sym">)
</span><span class="line">   52 </span><span class="sym"></span>            n <span class="sym">+=</span> <span class="kwa">len</span><span class="sym">(</span>dat<span class="sym">)
</span><span class="line">   53 </span><span class="sym"></span>        <span class="kwb">print</span> <span class="str">&quot;server processed %d bytes&quot;</span> <span class="sym">%</span> n
<span class="line">   54 </span>        os<span class="sym">.</span>wait<span class="sym">()
</span><span class="line">   55 </span><span class="sym"></span>    <span class="kwb">else</span><span class="sym">:
</span><span class="line">   56 </span><span class="sym"></span>        <span class="slc"># child is client
</span><span class="line">   57 </span><span class="slc"></span>        echo_client<span class="sym">(</span>n<span class="sym">,</span> get_port<span class="sym">(</span>ssock<span class="sym">))
</span><span class="line">   58 </span><span class="sym">
</span><span class="line">   59 </span><span class="sym"></span><span class="kwb">def</span> main<span class="sym">():
</span><span class="line">   60 </span><span class="sym"></span>    n <span class="sym">=</span> <span class="kwa">int</span><span class="sym">(</span>sys<span class="sym">.</span>argv<span class="sym">[</span><span class="num">1</span><span class="sym">])
</span><span class="line">   61 </span><span class="sym"></span>    <span class="kwb">if</span> n <span class="sym">&lt;</span> <span class="num">1</span><span class="sym">:
</span><span class="line">   62 </span><span class="sym"></span>        n <span class="sym">=</span> <span class="num">1
</span><span class="line">   63 </span><span class="num"></span>    echo_server<span class="sym">(</span>n<span class="sym">)
</span><span class="line">   64 </span><span class="sym">
</span><span class="line">   65 </span><span class="sym"></span>main<span class="sym">()</span>
