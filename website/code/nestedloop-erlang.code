<span class="slc">%%% -*- mode: erlang -*-
</span><span class="slc">%%% $Id: nestedloop-erlang.code,v 1.5 2005-03-22 09:05:10 bfulgham Exp $
</span><span class="slc">%%% http://www.bagley.org/~doug/shootout/
</span>
<span class="sym">-</span>module<span class="sym">(</span>nestedloop<span class="sym">)</span>.
<span class="sym">-</span>export<span class="sym">([</span>main<span class="sym">/</span><span class="num">1</span><span class="sym">])</span>.

main<span class="sym">([</span>Arg<span class="sym">]) -&gt;
</span>    Num <span class="sym">=</span> <span class="kwb">list_to_integer</span><span class="sym">(</span><span class="kwb">atom_to_list</span><span class="sym">(</span>Arg<span class="sym">)),
</span>    io<span class="sym">:</span>fwrite<span class="sym">(</span><span class="str">&quot;~w</span><span class="esc">\n</span><span class="str">&quot;</span><span class="sym">, [</span>loopA<span class="sym">(</span>Num<span class="sym">,</span> Num<span class="sym">,</span> <span class="num">0</span><span class="sym">)]),
</span>    <span class="kwb">halt</span><span class="sym">(</span><span class="num">0</span><span class="sym">)</span>.


loopA<span class="sym">(</span><span class="num">0</span><span class="sym">,</span> M<span class="sym">,</span> N<span class="sym">) -&gt;</span> N<span class="sym">;
</span>loopA<span class="sym">(</span>I<span class="sym">,</span> M<span class="sym">,</span> N<span class="sym">) -&gt;</span> loopA<span class="sym">(</span>I <span class="num">- 1</span><span class="sym">,</span> M<span class="sym">,</span> loopB<span class="sym">(</span>M<span class="sym">,</span> M<span class="sym">,</span> N<span class="sym">))</span>.

loopB<span class="sym">(</span><span class="num">0</span><span class="sym">,</span> M<span class="sym">,</span> N<span class="sym">) -&gt;</span> N<span class="sym">;
</span>loopB<span class="sym">(</span>I<span class="sym">,</span> M<span class="sym">,</span> N<span class="sym">) -&gt;</span> loopB<span class="sym">(</span>I <span class="num">- 1</span><span class="sym">,</span> M<span class="sym">,</span> loopC<span class="sym">(</span>M<span class="sym">,</span> M<span class="sym">,</span> N<span class="sym">))</span>.

loopC<span class="sym">(</span><span class="num">0</span><span class="sym">,</span> M<span class="sym">,</span> N<span class="sym">) -&gt;</span> N<span class="sym">;
</span>loopC<span class="sym">(</span>I<span class="sym">,</span> M<span class="sym">,</span> N<span class="sym">) -&gt;</span> loopC<span class="sym">(</span>I <span class="num">- 1</span><span class="sym">,</span> M<span class="sym">,</span> loopD<span class="sym">(</span>M<span class="sym">,</span> M<span class="sym">,</span> N<span class="sym">))</span>.

loopD<span class="sym">(</span><span class="num">0</span><span class="sym">,</span> M<span class="sym">,</span> N<span class="sym">) -&gt;</span> N<span class="sym">;
</span>loopD<span class="sym">(</span>I<span class="sym">,</span> M<span class="sym">,</span> N<span class="sym">) -&gt;</span> loopD<span class="sym">(</span>I <span class="num">- 1</span><span class="sym">,</span> M<span class="sym">,</span> loopE<span class="sym">(</span>M<span class="sym">,</span> M<span class="sym">,</span> N<span class="sym">))</span>.

loopE<span class="sym">(</span><span class="num">0</span><span class="sym">,</span> M<span class="sym">,</span> N<span class="sym">) -&gt;</span> N<span class="sym">;
</span>loopE<span class="sym">(</span>I<span class="sym">,</span> M<span class="sym">,</span> N<span class="sym">) -&gt;</span> loopE<span class="sym">(</span>I <span class="num">- 1</span><span class="sym">,</span> M<span class="sym">,</span> loopF<span class="sym">(</span>M<span class="sym">,</span> N<span class="sym">))</span>.

loopF<span class="sym">(</span><span class="num">0</span><span class="sym">,</span> N<span class="sym">) -&gt;</span> N<span class="sym">;
</span>loopF<span class="sym">(</span>I<span class="sym">,</span> N<span class="sym">) -&gt;</span> loopF<span class="sym">(</span>I <span class="num">- 1</span><span class="sym">,</span> <span class="num">1</span> <span class="sym">+</span> N<span class="sym">)</span>.
