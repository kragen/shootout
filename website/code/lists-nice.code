<table width="100%" border="0" cellpadding="5" cellspacing="0"><tr><td bgcolor="#FFFFFF"><pre>
<I><FONT COLOR="#B22222">/* The Great Computer Language Shootout 
   http://shootout.alioth.debian.org/
   contributed by Isaac Gouy

To compile:	
   nicec --sourcepath=.. -d=. -a lists.jar lists

To run:
   java -ea -jar lists.jar 16
*/</FONT></I>


<B><FONT COLOR="#A020F0">import</FONT></B> ackermann; <I><FONT COLOR="#B22222">// reuse toSinglePositiveInt
</FONT></I>
<B><FONT COLOR="#A020F0">void</FONT></B> main(String[] args){
   var n = toSinglePositiveInt(args);

   let nSize = 10000;
   var L1Count = 0;
   <B><FONT COLOR="#A020F0">while</FONT></B> (n-- &gt; 0){

      <I><FONT COLOR="#B22222">// initialize L1
</FONT></I>      let L1 = <B><FONT COLOR="#A020F0">new</FONT></B> LinkedList();
      <B><FONT COLOR="#A020F0">for</FONT></B> (var j = 1; j &lt;= nSize; j++) L1.add(j);

      <I><FONT COLOR="#B22222">// copy L1 to L2
</FONT></I>      let L2 = <B><FONT COLOR="#A020F0">new</FONT></B> LinkedList(L1);

      <I><FONT COLOR="#B22222">// remove from left of L2 add to right of L3
</FONT></I>      let L3 = <B><FONT COLOR="#A020F0">new</FONT></B> LinkedList();
      <B><FONT COLOR="#A020F0">while</FONT></B> (!L2.isEmpty) 
         L3.add( L2.removeFirst ); 

      <I><FONT COLOR="#B22222">// remove from right of L3 add to right of L2
</FONT></I>      <B><FONT COLOR="#A020F0">while</FONT></B> (!L3.isEmpty) 
         L2.add( L3.removeLast );

      <I><FONT COLOR="#B22222">// reverse L1
</FONT></I>      Collections.reverse(L1);

      <I><FONT COLOR="#B22222">// check that first item is now SIZE
</FONT></I>      <I><FONT COLOR="#B22222">// NOTE: no Object to int type cast needed
</FONT></I>      <B><FONT COLOR="#A020F0">if</FONT></B> (L1[0] != nSize) println(<B><FONT COLOR="#BC8F8F">&quot;First item of L1 != SIZE&quot;</FONT></B>);

      <I><FONT COLOR="#B22222">// check that L1 == L2
</FONT></I>      <B><FONT COLOR="#A020F0">if</FONT></B> ( !L1.equals(L2) ) println(<B><FONT COLOR="#BC8F8F">&quot;L1 != L2&quot;</FONT></B>);
      L1Count = L1.size;
      }

   println(L1Count);
}</pre></td></tr></table>
