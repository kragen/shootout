<table width="100%" border="0" cellpadding="5" cellspacing="0"><tr><td bgcolor="#FFFFFF"><pre>
<I><FONT COLOR="#B22222">/* The Great Computer Language Shootout 
   contributed by Isaac Gouy */</FONT></I>

using System;
using System.Collections;
using System.IO;
using System.Text;

class spellcheck
{
   <B><FONT COLOR="#228B22">static</FONT></B> <B><FONT COLOR="#228B22">void</FONT></B> Main(string[] args)
   {   
      <B><FONT COLOR="#228B22">const</FONT></B> <B><FONT COLOR="#228B22">int</FONT></B> bufferSize = 4096;
      Encoding code = Encoding.GetEncoding(0);
      Stream s = Console.OpenStandardInput( bufferSize );
      StreamReader stdin = new StreamReader(s, code, false, bufferSize);

      String word;
      Hashtable dictionary = new Hashtable(); 
      try {
         StreamReader sr = new StreamReader(<B><FONT COLOR="#BC8F8F">&quot;Usr.Dict.Words&quot;</FONT></B>, code, false, bufferSize);
         <B><FONT COLOR="#A020F0">while</FONT></B> ( (word = sr.ReadLine()) != null ) dictionary.Add(word, null);
         sr.Close();
      }
      catch (FileNotFoundException e) {
         Console.WriteLine(e.Message);
      }

      <B><FONT COLOR="#A020F0">while</FONT></B> ( (word = stdin.ReadLine()) != null ) 
         <B><FONT COLOR="#A020F0">if</FONT></B> (!dictionary.ContainsKey(word)) Console.WriteLine(word);
   }   
}</pre></td></tr></table>
