<table width="100%" border="0" cellpadding="5" cellspacing="0"><tr><td bgcolor="#FFFFFF"><pre>
<I><FONT COLOR="#B22222">#!/usr/bin/perl
</FONT></I><B><FONT COLOR="#A020F0">use</FONT></B> integer;

$n = $ARGV[0] || 1;
%X = ();
$c = 0;
<B><FONT COLOR="#A020F0">keys</FONT></B> %X=$n;
<B><FONT COLOR="#A020F0">for</FONT></B> ($i=0; $i&lt;$n;) {
    $X{<B><FONT COLOR="#A020F0">sprintf</FONT></B>(<B><FONT COLOR="#BC8F8F">'%x'</FONT></B>, $i)} = ++$i;
    $X{<B><FONT COLOR="#A020F0">sprintf</FONT></B>(<B><FONT COLOR="#BC8F8F">'%x'</FONT></B>, $i)} = ++$i;
    $X{<B><FONT COLOR="#A020F0">sprintf</FONT></B>(<B><FONT COLOR="#BC8F8F">'%x'</FONT></B>, $i)} = ++$i;
    $X{<B><FONT COLOR="#A020F0">sprintf</FONT></B>(<B><FONT COLOR="#BC8F8F">'%x'</FONT></B>, $i)} = ++$i;
}
<B><FONT COLOR="#A020F0">for</FONT></B> ($i=$n+1; $i&gt;0;) {
    $c+=<B><FONT COLOR="#A020F0">exists</FONT></B>($X{--$i}) + <B><FONT COLOR="#A020F0">exists</FONT></B>($X{--$i}) + <B><FONT COLOR="#A020F0">exists</FONT></B>($X{--$i}) + <B><FONT COLOR="#A020F0">exists</FONT></B>($X{--$i});
}
<B><FONT COLOR="#A020F0">print</FONT></B> $c,<B><FONT COLOR="#BC8F8F">&quot;\n&quot;</FONT></B>;</pre></td></tr></table>
