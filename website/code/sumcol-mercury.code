<span class="line">    1 </span><span class="slc">%% $Id: sumcol-mercury.code,v 1.3 2004-11-13 07:43:29 bfulgham Exp $
</span><span class="line">    2 </span><span class="slc"></span><span class="slc">%% http://www.bagley.org/~doug/shootout/
</span><span class="line">    3 </span><span class="slc"></span>
<span class="line">    4 </span><span class="sym">:-</span> module mytest.
<span class="line">    5 </span>
<span class="line">    6 </span><span class="sym">:-</span> interface.
<span class="line">    7 </span>
<span class="line">    8 </span><span class="sym">:-</span> import_module io.
<span class="line">    9 </span>
<span class="line">   10 </span><span class="sym">:-</span> pred main<span class="sym">(</span>io__state <span class="sym">::</span> di<span class="sym">,</span> io__state <span class="sym">::</span> uo<span class="sym">)</span> <span class="kwa">is</span> det.
<span class="line">   11 </span>
<span class="line">   12 </span><span class="sym">:-</span> implementation.
<span class="line">   13 </span>
<span class="line">   14 </span><span class="sym">:-</span> import_module string<span class="sym">,</span> int.
<span class="line">   15 </span>
<span class="line">   16 </span>main <span class="sym">--&gt;
</span><span class="line">   17 </span><span class="sym"></span>    io__read_line_as_string<span class="sym">(</span>IOResult<span class="sym">),
</span><span class="line">   18 </span><span class="sym"></span>    sumcol<span class="sym">(</span>IOResult<span class="sym">,</span> <span class="num">0</span><span class="sym">)</span>.
<span class="line">   19 </span>
<span class="line">   20 </span><span class="sym">:-</span> pred sumcol<span class="sym">(</span>io__result<span class="sym">(</span>string<span class="sym">)::</span>in<span class="sym">,</span> int<span class="sym">::</span>in<span class="sym">,</span> io__state<span class="sym">::</span>di<span class="sym">,</span> io__state<span class="sym">::</span>uo<span class="sym">)</span> <span class="kwa">is</span> det.
<span class="line">   21 </span>
<span class="line">   22 </span>sumcol<span class="sym">(</span>IOResult<span class="sym">,</span> Sum<span class="sym">) --&gt;
</span><span class="line">   23 </span><span class="sym">    ( {</span> IOResult <span class="sym">=</span> ok<span class="sym">(</span>LineIn<span class="sym">) },
</span><span class="line">   24 </span><span class="sym">        {</span> chomp<span class="sym">(</span>LineIn<span class="sym">,</span> Line<span class="sym">) },
</span><span class="line">   25 </span><span class="sym"></span>        io__read_line_as_string<span class="sym">(</span>NewIOResult<span class="sym">),
</span><span class="line">   26 </span><span class="sym">        (</span> if <span class="sym">{</span> string__to_int<span class="sym">(</span>Line<span class="sym">,</span> N<span class="sym">) }</span> then
<span class="line">   27 </span>            sumcol<span class="sym">(</span>NewIOResult<span class="sym">,</span> Sum <span class="sym">+</span> N<span class="sym">)
</span><span class="line">   28 </span><span class="sym"></span>          else
<span class="line">   29 </span>            sumcol<span class="sym">(</span>NewIOResult<span class="sym">,</span> Sum<span class="sym">)
</span><span class="line">   30 </span><span class="sym">        )
</span><span class="line">   31 </span><span class="sym">    ; {</span> IOResult <span class="sym">=</span> eof <span class="sym">},
</span><span class="line">   32 </span><span class="sym"></span>        io__write_int<span class="sym">(</span>Sum<span class="sym">),
</span><span class="line">   33 </span><span class="sym"></span>        io__write_string<span class="sym">(</span><span class="str">&quot;</span><span class="esc">\n</span><span class="str">&quot;</span><span class="sym">)
</span><span class="line">   34 </span><span class="sym">    ; {</span> IOResult <span class="sym">=</span> error<span class="sym">(</span>_Error<span class="sym">) },
</span><span class="line">   35 </span><span class="sym"></span>        io__write_string<span class="sym">(</span><span class="str">&quot;Error reading file!&quot;</span><span class="sym">)
</span><span class="line">   36 </span><span class="sym">    )</span>.
<span class="line">   37 </span>
<span class="line">   38 </span><span class="sym">:-</span> pred chomp<span class="sym">(</span>string<span class="sym">::</span>in<span class="sym">,</span> string<span class="sym">::</span>out<span class="sym">)</span> <span class="kwa">is</span> det.
<span class="line">   39 </span>
<span class="line">   40 </span>chomp<span class="sym">(</span>InStr<span class="sym">,</span> OutStr<span class="sym">) :-
</span><span class="line">   41 </span><span class="sym">    (</span> if string__remove_suffix<span class="sym">(</span>InStr<span class="sym">,</span> <span class="str">&quot;</span><span class="esc">\n</span><span class="str">&quot;</span><span class="sym">,</span> NewStr<span class="sym">)</span> then
<span class="line">   42 </span>        OutStr <span class="sym">=</span> NewStr
<span class="line">   43 </span>      else
<span class="line">   44 </span>        OutStr <span class="sym">=</span> InStr
<span class="line">   45 </span>    <span class="sym">)</span>.
