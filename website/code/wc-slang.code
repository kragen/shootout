define main<span class="sym">()
{
</span>   variable nl<span class="sym">,</span> nw<span class="sym">,</span> nc<span class="sym">;

</span>   nl <span class="sym">=</span> <span class="num">0</span><span class="sym">;</span> nw <span class="sym">=</span> <span class="num">0</span><span class="sym">;</span> nc <span class="sym">=</span> <span class="num">0</span><span class="sym">;

</span>   foreach <span class="sym">(</span>stdin<span class="sym">)</span> <span class="kwa">using</span> <span class="sym">(</span><span class="str">&quot;line&quot;</span><span class="sym">)
     {
</span>	variable line <span class="sym">= ();
</span>        nl<span class="sym">++;
</span>	nc <span class="sym">+=</span> strlen <span class="sym">(</span>line<span class="sym">);
</span>	nw <span class="sym">+=</span> length <span class="sym">(</span>strtok <span class="sym">(</span>line<span class="sym">));
     }

</span>   fprintf <span class="sym">(</span>stdout<span class="sym">,</span> <span class="str">&quot;%d %d %d</span><span class="esc">\n</span><span class="str">&quot;</span><span class="sym">,</span> nl<span class="sym">,</span> nw<span class="sym">,</span> nc<span class="sym">);
}

</span>main<span class="sym">()</span>
