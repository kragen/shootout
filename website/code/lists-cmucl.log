BUILD COMMANDS FOR: lists.cmucl

Fri Nov 11 22:14:06 PST 2005

CMUCL built with: /usr/bin/lisp -noinit -batch -eval '(load "lists.cmucl_compile")'
### START lists.cmucl_compile
(proclaim '(optimize (speed 3) (safety 0) (debug 0) (compilation-speed 0) (space 0)))
(compile-file "lists.cmucl" :block-compile t  :entry-points '(main))
(quit)
### END lists.cmucl_compile

; Loading #P"/opt/shootout/shootout/bench/lists/tmp/lists.cmucl_compile".

; Python version 1.1, VM version Intel x86 on 11 NOV 05 10:14:06 pm.
; Compiling: /opt/shootout/shootout/bench/lists/tmp/lists.cmucl 11 NOV 05 10:14:06 pm

; Converted XCONS.
; Converted XPOP.
; Converted PUSH-QUEUE.
; Converted WITH-COLLECTOR.
; Converted TEST-LIST.
; Converted MAIN.
; Compiling DEFMACRO XPOP: 
; Compiling DEFMACRO WITH-COLLECTOR: 
; Compiling DEFUN MAIN: 

; 
; 
; File: /opt/shootout/shootout/bench/lists/tmp/lists.cmucl

; In: DEFUN MAIN

;   (LOOP REPEAT N DO #)
; --> BLOCK LET ANSI-LOOP::LOOP-BODY TAGBODY WHEN COND IF MINUSP < DECF LET* 
; ==>
;   (- #:G91 #:G93)
; Note: Unable to optimize due to type uncertainty:
;     The first argument is a REAL, not a FLOAT.
; 
; --> BLOCK LET ANSI-LOOP::LOOP-BODY TAGBODY WHEN COND IF MINUSP 
; ==>
;   (< (DECF #:G91) 0)
; Note: Unable to optimize due to type uncertainty:
;     The first argument is a REAL, not a FLOAT.
; 

; In: DEFUN TEST-LIST

;   (= (THE FIXNUM #) *SIZE*)
; Note: Unable to optimize due to type uncertainty:
;     The second argument is a NUMBER, not a (COMPLEX SINGLE-FLOAT).
; 
; Note: Unable to optimize due to type uncertainty:
;     The second argument is a NUMBER, not a (COMPLEX DOUBLE-FLOAT).
; 
; Note: Unable to open code because:
;     Operands might not be the same type.
; 

; In: DEFUN MAIN

;   (LOOP REPEAT N DO #)
; --> BLOCK LET ANSI-LOOP::LOOP-BODY TAGBODY WHEN COND IF MINUSP < DECF LET* 
; ==>
;   (- #:G91 #:G93)
; Note: Forced to do GENERIC-- (cost 10).
;     Unable to do inline fixnum arithmetic (cost 1) because:
;     The first argument is a REAL, not a FIXNUM.
;     The result is a REAL, not a FIXNUM.
;     Unable to do inline fixnum arithmetic (cost 2) because:
;     The first argument is a REAL, not a FIXNUM.
;     The result is a REAL, not a FIXNUM.
;     etc.
; 
; --> BLOCK LET ANSI-LOOP::LOOP-BODY TAGBODY WHEN COND IF MINUSP 
; ==>
;   (< (DECF #:G91) 0)
; Note: Forced to do GENERIC-< (cost 10).
;     Unable to do inline fixnum comparison (cost 3) because:
;     The first argument is a REAL, not a FIXNUM.
;     Unable to do inline fixnum comparison (cost 4) because:
;     The first argument is a REAL, not a FIXNUM.
;     etc.
; 
; Byte Compiling Top-Level Form: 

; Compilation unit finished.
;   7 notes


; lists.x86f written.
; Compilation finished in 0:00:00.
### START lists.cmucl_run
(proclaim '(optimize (speed 3) (safety 0) (debug 0) (compilation-speed 0) (space 0)))
(setq *gc-verbose* nil)
(load "lists.x86f" :verbose nil :print nil)
(main) (quit)
### END lists.cmucl_run


=================================================================
COMMAND LINE (%A is single numeric argument):

 /usr/bin/lisp -noinit -batch -eval '(load "lists.cmucl_run" :verbose nil :print nil)' %A


PROGRAM OUTPUT
==============
10000
