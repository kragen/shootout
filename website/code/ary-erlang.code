<span class="line">    1 </span><span class="slc">%%% -*- mode: erlang -*-
</span><span class="line">    2 </span><span class="slc"></span><span class="slc">%%% $Id: ary-erlang.code,v 1.4 2004-11-13 07:40:58 bfulgham Exp $
</span><span class="line">    3 </span><span class="slc"></span><span class="slc">%%% http://www.bagley.org/~doug/shootout/
</span><span class="line">    4 </span><span class="slc"></span><span class="slc">%%% from James Hague
</span><span class="line">    5 </span><span class="slc"></span>
<span class="line">    6 </span><span class="sym">-</span>module<span class="sym">(</span>ary<span class="sym">)</span>.
<span class="line">    7 </span><span class="sym">-</span>export<span class="sym">([</span>main<span class="sym">/</span><span class="num">1</span><span class="sym">])</span>.
<span class="line">    8 </span>
<span class="line">    9 </span>main<span class="sym">() -&gt;</span> main<span class="sym">([</span><span class="str">'1'</span><span class="sym">])</span>.
<span class="line">   10 </span>main<span class="sym">([</span>Arg<span class="sym">]) -&gt;
</span><span class="line">   11 </span><span class="sym"></span>    Num <span class="sym">=</span> <span class="kwb">list_to_integer</span><span class="sym">(</span><span class="kwb">atom_to_list</span><span class="sym">(</span>Arg<span class="sym">)),
</span><span class="line">   12 </span><span class="sym"></span>    ets<span class="sym">:</span>new<span class="sym">(</span>y<span class="sym">, [</span>set<span class="sym">,</span> private<span class="sym">,</span> named_table<span class="sym">]),
</span><span class="line">   13 </span><span class="sym"></span>    clear_y_array<span class="sym">(</span>Num<span class="sym">),
</span><span class="line">   14 </span><span class="sym"></span>    X <span class="sym">=</span> <span class="kwb">list_to_tuple</span><span class="sym">(</span>lists<span class="sym">:</span>seq<span class="sym">(</span><span class="num">1</span><span class="sym">,</span> Num<span class="sym">)),
</span><span class="line">   15 </span><span class="sym"></span>    repeat<span class="sym">(</span>X<span class="sym">,</span> Num<span class="sym">,</span> <span class="num">1000</span><span class="sym">),
</span><span class="line">   16 </span><span class="sym">    [{</span>_<span class="sym">,</span>First<span class="sym">}] =</span> ets<span class="sym">:</span>lookup<span class="sym">(</span>y<span class="sym">,</span> <span class="num">1</span><span class="sym">),
</span><span class="line">   17 </span><span class="sym">    [{</span>_<span class="sym">,</span>Last<span class="sym">}]  =</span> ets<span class="sym">:</span>lookup<span class="sym">(</span>y<span class="sym">,</span> Num<span class="sym">),
</span><span class="line">   18 </span><span class="sym"></span>    io<span class="sym">:</span>fwrite<span class="sym">(</span><span class="str">&quot;~w ~w~n&quot;</span><span class="sym">, [</span>First<span class="sym">,</span> Last<span class="sym">]),
</span><span class="line">   19 </span><span class="sym"></span>    ets<span class="sym">:</span>delete<span class="sym">(</span>y<span class="sym">),
</span><span class="line">   20 </span><span class="sym"></span>    <span class="kwb">halt</span><span class="sym">(</span><span class="num">0</span><span class="sym">)</span>.
<span class="line">   21 </span>
<span class="line">   22 </span>clear_y_array<span class="sym">(</span><span class="num">0</span><span class="sym">) -&gt;</span> ok<span class="sym">;
</span><span class="line">   23 </span><span class="sym"></span>clear_y_array<span class="sym">(</span>I<span class="sym">) -&gt;
</span><span class="line">   24 </span><span class="sym"></span>    ets<span class="sym">:</span>insert<span class="sym">(</span>y<span class="sym">, {</span>I<span class="sym">,</span><span class="num">0</span><span class="sym">}),
</span><span class="line">   25 </span><span class="sym"></span>    clear_y_array<span class="sym">(</span>I <span class="num">- 1</span><span class="sym">)</span>.
<span class="line">   26 </span>
<span class="line">   27 </span>repeat<span class="sym">(</span>X<span class="sym">,</span> N<span class="sym">,</span> <span class="num">0</span><span class="sym">) -&gt;</span> ok<span class="sym">;
</span><span class="line">   28 </span><span class="sym"></span>repeat<span class="sym">(</span>X<span class="sym">,</span> N<span class="sym">,</span> K<span class="sym">) -&gt;
</span><span class="line">   29 </span><span class="sym"></span>    calc<span class="sym">(</span>X<span class="sym">,</span> N<span class="sym">),
</span><span class="line">   30 </span><span class="sym"></span>    repeat<span class="sym">(</span>X<span class="sym">,</span> N<span class="sym">,</span> K <span class="num">- 1</span><span class="sym">)</span>.
<span class="line">   31 </span>
<span class="line">   32 </span>calc<span class="sym">(</span>X<span class="sym">,</span> <span class="num">0</span><span class="sym">) -&gt;</span> ok<span class="sym">;
</span><span class="line">   33 </span><span class="sym"></span>calc<span class="sym">(</span>X<span class="sym">,</span> N<span class="sym">) -&gt;
</span><span class="line">   34 </span><span class="sym"></span>    ets<span class="sym">:</span>update_counter<span class="sym">(</span>y<span class="sym">,</span> N<span class="sym">,</span> <span class="kwb">element</span><span class="sym">(</span>N<span class="sym">,</span> X<span class="sym">)),
</span><span class="line">   35 </span><span class="sym"></span>    calc<span class="sym">(</span>X<span class="sym">,</span> N <span class="num">- 1</span><span class="sym">)</span>.
