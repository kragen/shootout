<table width="100%" border="0" cellpadding="5" cellspacing="0"><tr><td bgcolor="#FFFFFF"><pre>
<I><FONT COLOR="#B22222">/* The Great Computer Language Shootout 
   http://shootout.alioth.debian.org/
   contributed by Isaac Gouy

To compile:	
   nicec --sourcepath=.. -d=. -a spellcheck.jar spellcheck

To run:
   java -jar spellcheck.jar &lt; input.txt &gt; out.txt
*/</FONT></I>


<B><FONT COLOR="#A020F0">import</FONT></B> java.io.*;

<B><FONT COLOR="#A020F0">void</FONT></B> main(String[] args){
   let dictionary = <B><FONT COLOR="#A020F0">new</FONT></B> HashMap();

   <B><FONT COLOR="#A020F0">try</FONT></B> {
      let f = <B><FONT COLOR="#A020F0">new</FONT></B> BufferedReader(<B><FONT COLOR="#A020F0">new</FONT></B> FileReader(<B><FONT COLOR="#BC8F8F">&quot;Usr.Dict.Words&quot;</FONT></B>));
      f.foreach(String word =&gt; { dictionary.put(word, 1); });
      f.close;
   } 
   <B><FONT COLOR="#A020F0">catch</FONT></B> (IOException e) { 
      System.err.println(e); 
      <B><FONT COLOR="#A020F0">return</FONT></B>; 
   }

   <B><FONT COLOR="#A020F0">try</FONT></B> {
      let r = <B><FONT COLOR="#A020F0">new</FONT></B> BufferedReader(<B><FONT COLOR="#A020F0">new</FONT></B> InputStreamReader(System.in));
      r.foreach(String word =&gt; { 
         <B><FONT COLOR="#A020F0">if</FONT></B> (!dictionary.containsKey(word)) println(word); });
   } 
   <B><FONT COLOR="#A020F0">catch</FONT></B> (IOException e) { 
      System.err.println(e); 
   }
}


<B><FONT COLOR="#A020F0">void</FONT></B> foreach(BufferedReader r, String -&gt; <B><FONT COLOR="#A020F0">void</FONT></B> expr) { 
   var ?String s;
   <B><FONT COLOR="#A020F0">while</FONT></B> ((s = r.readLine()) != <B><FONT COLOR="#A020F0">null</FONT></B>) expr(s);
}</pre></td></tr></table>
