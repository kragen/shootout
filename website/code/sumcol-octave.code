# The <span class="kwa">Computer</span> Language Shootout
# http<span class="sym">://</span>shootout.alioth.debian.org<span class="sym">/</span>
# contributed by Carson Reynolds <span class="num">2007</span><span class="sym">/</span><span class="num">4</span><span class="sym">/</span><span class="num">10</span>

y <span class="sym">=</span> <span class="num">0</span><span class="sym">;</span>
x <span class="sym">=</span> <span class="kwb">fgets</span><span class="sym">(</span>stdin<span class="sym">);</span>

<span class="kwa">while</span> ~<span class="kwb">feof</span><span class="sym">(</span>stdin<span class="sym">)</span>
  y <span class="sym">=</span> y <span class="sym">+</span> <span class="kwb">str2num</span><span class="sym">(</span>x<span class="sym">);</span>
  x <span class="sym">=</span> <span class="kwb">fgets</span><span class="sym">(</span>stdin<span class="sym">);</span>
<span class="kwa">end</span>

printf<span class="sym">(</span><span class="str">&quot;%d\n&quot;</span><span class="sym">,</span> y<span class="sym">);</span>
