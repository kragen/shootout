<span class="slc">;;; The Computer Language Shootout</span>
<span class="slc">;;; http://shootout.alioth.debian.org/</span>
<span class="slc">;;;</span>
<span class="slc">;;; Original for mzscheme contributed by Eli Barzilay.</span>
<span class="slc">;;; Adapted for gambit scheme by Vincent Kraeutler.</span>

<span class="sym">(</span>let addLine <span class="sym">((</span>sum <span class="num">0</span><span class="sym">))</span>
  <span class="sym">(</span>let <span class="sym">((</span>nn <span class="sym">(</span><span class="kwa">read</span><span class="sym">)))</span>
  <span class="sym">(</span><span class="kwa">if</span> <span class="sym">(</span>eof<span class="sym">-</span>object? nn<span class="sym">)</span>
    <span class="sym">(</span>begin
      <span class="sym">(</span>display sum<span class="sym">)</span>
      <span class="sym">(</span>newline<span class="sym">))</span>
    <span class="sym">(</span>addLine <span class="sym">(+</span> sum nn<span class="sym">)))))</span>
