;;; The Computer Language Shootout 
;;; http://shootout.alioth.debian.org/
;;;
;;; Original for mzscheme contributed by Eli Barzilay.
;;; Adapted for gambit scheme by Vincent Kraeutler.

(let addLine ((sum 0))
  (let ((nn (read)))
  (if (eof-object? nn)
    (begin 
      (display sum)
      (newline))
    (addLine (+ sum nn)))))
