# The Computer Language Shootout
# http://shootout.alioth.debian.org/
# contributed by Carson Reynolds 2007/3/15

v = [];
x = fgets(stdin);

while ~feof(stdin)
  v = [v str2num(x)];
  x = fgets(stdin);
end

printf("%d\n", sum(v));