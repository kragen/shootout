#!/usr/bin/mzscheme -r

(define (main)
  (let ((sum 0))
    (do ((myInt (read-line) (read-line)))
	  ((eof-object? myInt) (printf "~a~n" sum))
      (set! sum (+ sum (string->number myInt))))))

(main)
