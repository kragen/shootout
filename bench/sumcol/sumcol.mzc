#!/usr/bin/mzscheme -rqxA
;;; Sumcol
;;; http://shootout.alioth.debian.org/
;;;
;;; Contributed by Eli Barzilay
(let loop ([acc 0])
  (let ([n (read)])
    (if (eof-object? n)
        (printf "~a\n" acc)
        (loop (+ acc n)))))
