//
// Contributed by John Skaller
//
include "std";
n := atoi(System::argv 1);

open Text_file;
var sum = 0;
var line = readln stdin;

while {line != "" } {
  word := line.[to -1];
  sum += atoi(word);
  line = readln stdin;
};

print sum; endl;


