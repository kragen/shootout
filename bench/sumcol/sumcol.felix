#line 354 "lpsrc/flx_bagley.ipk"
include "std";
//
//  Contributed by John Skaller
//
val n = atoi(System::argv 1);
//print "sumcol"; print n; endl;

open Text_file;
var sum = 0;
var line = readln stdin;

while {line != "" } {
  word := line.[to -1];
  sum += atoi(word);
  line = readln stdin;
};

print sum; endl;


