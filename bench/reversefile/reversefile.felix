#line 258 "lpsrc/flx_bagley.ipk"
include "std";
//
//  Contributed by John Skalelr
//
val n = atoi(System::argv 1);
//print "revfile "; print n; endl;

open List;
open Text_file;

var x = Empty[string];
var s = readln stdin;
while { s != "" }
{
  x = Cons (s,x);
  s = readln stdin;
};

proc plist(x:list[string]){
  match x with
  | Cons (?s,?t) => { print s; plist t; }
  |  _ => {}
  endmatch;
};

plist x; endl;

