#!/usr/bin/python
# $Id: spellcheck.psyco,v 1.1 2004-12-05 01:59:53 bfulgham Exp $
# http://www.bagley.org/~doug/shootout/
# From Fred Bremmer

import sys, psyco
psyco.full()

def main():
    dict = {}
    dict_has_key = dict.has_key
    for line in open("Usr.Dict.Words").xreadlines():
        word = line[:-1]
        if word: dict[word] = 1

    for line in sys.stdin.xreadlines():
        word = line[:-1]
        if word:
            if not dict_has_key(word): print word

main()
