#!/usr/bin/newlisp

# count benchmark

(set 'lc 0 'wc 0 'cc 0)

(while (set 'line (read-line))
	(inc 'lc)
	(inc 'wc (length (parse line)))
	(inc 'cc (length line)))

(println lc " " wc " " cc)

(exit)

