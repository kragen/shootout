#line 41 "lpsrc/flx_bagley.ipk"
//
//  Contributed by John Skaller
//
include "std";
val n = atoi(System::argv 1);
// print "fib "; print n; endl;

fun fib(n:int):int =>
  if n < 2 then 1 else fib (n - 2) + fib (n - 1) endif
;

val v = fib(n);
//print "Fib("; print n; print "): "; print v; endl;
print v; endl;

