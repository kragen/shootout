#!/bin/bash
# Isaac Gouy 18 Nov 2005

# make timestamp, so later we can check which logfiles were modified
# Of course, we won't need to read the file to get the timestamp,
# we can just use the file's modification timestamp

date +'%s' > "timestamp"
